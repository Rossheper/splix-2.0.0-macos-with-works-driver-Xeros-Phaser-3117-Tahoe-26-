Форк репозитория janrueth/splix-2.0.0-macos.

Успешно пересобран под свой Macbook Pro 14 M1 Pro (Tahoe 26.0.1) для подключения принтера Xerox Phaser 3117.

Для сборки необходимо установить:
xcode-select --install
Homebrew (на сайте скопировать команду в terminal).

После установки homebrew terminal надо перезапустить.

Команды для сборки:

Если принтер использует jbig:
brew install jbigkit
sudo make install

Если принтер НЕ использует jbig:
make DISABLE_JBIG=1
sudo make install
